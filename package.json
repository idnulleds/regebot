{
  "name": "whatsapp-web-api",
  "version": "1.0.0",
  "main": "api/index.js",
  "scripts": {
    "start": "node api/index.js",         // Menjalankan aplikasi secara manual
    "build": "npm install && npm start", // Memastikan install dependencies & menjalankan aplikasi
    "vercel-build": "npm install && npm start" // Skrip untuk Vercel otomatis menjalankan aplikasi
  },
  "dependencies": {
    "whatsapp-web.js": "^1.18.0",
    "qrcode-terminal": "^0.12.0"
  },
  "engines": {
    "node": "20.x" // Memastikan menggunakan Node.js versi 20
  }
}
