{
  "name": "whatsapp-web-api",
  "version": "1.0.0",
  "main": "api/index.js",
  "scripts": {
    "start": "node api/index.js",         
    "build": "npm install && npm start", 
    "vercel-build": "npm install && npm start" 
  },
  "dependencies": {
    "whatsapp-web.js": "^1.18.0",
    "qrcode-terminal": "^0.12.0"
  },
  "engines": {
    "node": "20.x" 
  }
}
